/*
 * This source file was generated by the Gradle 'init' task
 */
package edu.curso;

import javafx.application.Application;
import javafx.event.EventHandler;
import javafx.geometry.Pos;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.input.MouseEvent;
import javafx.scene.layout.BorderPane;
import javafx.scene.layout.HBox;
import javafx.scene.layout.VBox;
import javafx.stage.Stage;



// class AssinanteSubTituloClique implements EventHandler<MouseEvent> { 
//     public void handle(MouseEvent e) { 
//         System.out.println("Clicado no SubTitulo");
//     }
// }


public class AppTesteEventosClasseAnonima extends Application {

    private Label lblTitulo;
    private Label lblSubTitulo;

    // class AssinanteClique implements EventHandler<MouseEvent> { 
    //     public void handle(MouseEvent e) { 
    //         if (e.getSource() == lblTitulo) {
    //             System.out.println("Clicado no Titulo");
    //         } else if (e.getSource() == lblSubTitulo) {
    //             System.out.println("Clicado no SubTitulo");
    //         }    
    //     }
    // }

    @Override
    public void start(Stage stage) { 
        BorderPane painel = new BorderPane();

        lblTitulo = new Label("Titulo");
        lblTitulo.setStyle("-fx-font-size: 32");
        
        EventHandler<MouseEvent> as1 = new EventHandler<>() { 
            public void handle(MouseEvent e) { 
                System.out.println("Clicado no Titulo");
            }
        };

        EventHandler<MouseEvent> as2 = new EventHandler<>() { 
            public void handle(MouseEvent e) { 
                System.out.println("Clicado no SubTitulo");
            }
        };

        lblTitulo.addEventHandler(MouseEvent.MOUSE_CLICKED, as1);

        lblSubTitulo = new Label("Sub Titulo");
        lblSubTitulo.setStyle("-fx-font-size: 28");
        lblSubTitulo.addEventHandler(MouseEvent.MOUSE_CLICKED, as2);

        Button btnSalvar = new Button("Salvar");
        Button btnPesquisar = new Button("Pesquisar");
        VBox titulos  = new VBox(lblTitulo, lblSubTitulo);
        titulos.setAlignment(Pos.CENTER);
        painel.setTop( titulos );

        HBox botoes = new HBox(30.0, btnSalvar, btnPesquisar);
        botoes.setAlignment(Pos.CENTER);
        painel.setBottom( botoes );

        BorderPane.setAlignment(titulos, Pos.CENTER);
        BorderPane.setAlignment(botoes, Pos.CENTER);

        Scene scn = new Scene(painel, 600, 400);

        stage.setScene(scn);
        stage.show();
    }

    public static void main(String[] args) {
        Application.launch(App.class, args);
    }
}
